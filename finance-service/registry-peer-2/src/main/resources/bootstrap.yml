#Service Registry properties are grabbed from External Configuration Server
#Be aware about Configuration Server should be up before starting Registry Server
spring:
  application:
    name: registry-peer-2
  cloud:
    config:
      uri: http://config-server:8888/config-server
      fail-fast: true