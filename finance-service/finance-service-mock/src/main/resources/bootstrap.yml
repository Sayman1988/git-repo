# Finance service properties are grabbed from External Configuration Server
# Be aware about Configuration Server should be up before starting Finance service
spring:
  application:
    name: finance-service-mock
  cloud:
    config:
      uri: http://config-server:8080/config-server
      fail-fast: true