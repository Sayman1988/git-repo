# Notification service properties are grabbed from External Configuration Server
# Be aware about Configuration Server should be up before starting Notification service
spring:
  application:
    name: notification-service-mock
  cloud:
    config:
      uri: http://config-server:8080/config-server
      fail-fast: true