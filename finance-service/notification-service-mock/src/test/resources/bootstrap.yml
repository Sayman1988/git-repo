eureka:
  client:
    enabled: false

spring:
  cloud:
    config:
      enabled: false
  application:
    name: notification-service-mock-test
    instance_id: test_instance_id

random:
  value: test_value