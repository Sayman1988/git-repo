# Monitoring properties are grabbed from External Configuration Server
# Be aware about Configuration Server should be up before starting Monitoring
spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://config-server:8080/config-server
      fail-fast: true